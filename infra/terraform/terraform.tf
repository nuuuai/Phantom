/**
 * Phantom — Terraform root (placeholder).
 * Add providers, backend, and modules when AWS resources are ready.
 * See README.md and docs/roadmap/INFRA_AWS_PHASE1.md.
 */
terraform {
  required_version = ">= 1.5.0"
}
