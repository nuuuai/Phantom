import { PlaceholderPage } from "@/features/placeholder/PlaceholderPage.js";

export function VaultPage() {
  return (
    <PlaceholderPage
      title="Vault"
      description="Zero-knowledge vault: unlock state and sync health only in this console; secrets stay client-side."
    />
  );
}
