import { PlaceholderPage } from "@/features/placeholder/PlaceholderPage.js";

export function AliasesPage() {
  return (
    <PlaceholderPage
      title="Aliases"
      description="Shield identities, rotation, and forwarding. Full list and detail views ship in Phase 1 Month 2."
    />
  );
}
