import { PlaceholderPage } from "@/features/placeholder/PlaceholderPage.js";

export function BrokerRemovalPage() {
  return (
    <PlaceholderPage
      title="Broker removal"
      description="Opt-out submissions, confirmations, and re-listing monitors across the broker registry."
    />
  );
}
